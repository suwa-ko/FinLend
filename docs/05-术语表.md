# 05 · 术语表（附录）

> 符号说明：标 **⚠️** 的词是**本项目不采用**的技术，列出它们是为了说明「为什么不用」。

---

## 1. 如何使用本文档

- 全文按主题分组（架构 / 后端 / 前端 / 数据库 / 安全 / 工程 / 业务 / 测试）。
- 开发者可以借由此表快速查阅项目相关的术语

---

## 2. 架构与开发模式

| 术语         | 英文/全称                         | 通俗解释                                                                             | 在本项目中                                 |
| ------------ | --------------------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------ |
| 前端         | Front-end                         | 用户在浏览器里看到并操作的那部分界面                                                 | Vue 3 写的部分                             |
| 后端         | Back-end                          | 跑在服务器上、负责数据与业务规则的那部分                                             | FastAPI 写的部分                           |
| 前后端分离   | Front-end / Back-end Separation   | 把「界面」和「业务逻辑」拆成两个独立程序，各自开发、通过网络通信                     | 本项目的**总体架构**                       |
| 接口 / API   | Application Programming Interface | 两个程序约定好的「对话方式」：一方按格式发请求，另一方按格式回数据                   | 前端通过 API 向后端要数据                  |
| RESTful      | Representational State Transfer   | 一种主流的 API 设计风格：把数据看作「资源」，用 GET/POST/PUT/DELETE 等标准动作操作它 | 本项目所有后端接口的设计风格               |
| HTTP / HTTPS | HyperText Transfer Protocol       | 浏览器与服务器之间传数据的「普通话」；HTTPS 是它的加密版                             | 前后端通过 HTTP 传 JSON                    |
| JSON         | JavaScript Object Notation        | 一种人和机器都能看懂的文本数据格式，形如 `{"name":"张三","age":20}`                  | 前后端之间传递的数据格式                   |
| 单页应用     | SPA, Single Page Application      | 整个网站只有一个网页，切页时不重新加载，由 JS 动态替换内容                           | 本项目前端就是 SPA                         |
| 单体架构     | Monolith                          | 所有功能放在一个程序里一起部署                                                       | 本项目后端采用单体，不用微服务             |
| 分层架构     | Layered Architecture              | 把代码按职责分层，上层调用下层，各层只管自己的事                                     | 路由层 → 业务层 → 数据访问层               |
| 接口契约     | API Contract                      | 前后端事先约好「请求长什么样、响应长什么样」，之后各自照此开发                       | 约定后再并行开发，避免返工                 |
| 脚手架       | Scaffold / Boilerplate            | 项目刚创建时的基础骨架代码，含基本配置                                               | 第 1 天要搭的「最小可运行骨架」            |
| 中间件       | Middleware                        | 在请求被真正处理前后插入的一段公共逻辑（如鉴权、日志）                               | 统一鉴权、统一异常处理                     |
| 跨域 / CORS  | Cross-Origin Resource Sharing     | 浏览器的安全限制：不同地址的网页默认不能互相请求；CORS 是服务端「开白名单」的机制    | 前后端分开部署时需配置                     |
| CRUD         | Create / Read / Update / Delete   | 增、查、改、删四种最基础的数据操作                                                   | 产品管理等模块本质就是 CRUD                |
| 微服务       | Microservices                     | 把系统拆成很多个小服务，各自独立部署                                                 | ⚠️ 不采用：业务规模小，单体足够            |
| 消息队列     | Message Queue                     | 高并发时用来「排队削峰」的组件                                                       | ⚠️ 不采用：无高并发需求                    |
| 分布式事务   | Distributed Transaction           | 跨多个数据库/服务保证数据一致                                                        | ⚠️ 不采用：单库事务已足够                  |
| 规则引擎     | Rule Engine                       | 把业务规则抽出来单独配置和执行的组件                                                 | ⚠️ 不采用：风控规则简单，代码 + 配置表即可 |

---

## 3. 后端技术

| 术语                 | 英文/全称                             | 通俗解释                                                   | 在本项目中                   |
| -------------------- | ------------------------------------- | ---------------------------------------------------------- | ---------------------------- |
| FastAPI              | —                                     | Python 的现代 Web 框架，写接口简洁、自带交互式文档         | 本项目后端框架               |
| Uvicorn              | —                                     | 用来运行 FastAPI 应用的服务器程序                          | 启动后端服务                 |
| ASGI                 | Asynchronous Server Gateway Interface | Python 异步 Web 服务器的标准接口                           | FastAPI / Uvicorn 的技术基础 |
| 虚拟环境             | venv                                  | 给每个项目单独隔离一套 Python 依赖，互不干扰               | 后端开发第一步就是创建 venv  |
| 依赖                 | Dependency                            | 项目用到的第三方代码库                                     | `requirements.txt` 里列的包  |
| pip / uv / poetry    | —                                     | Python 的包管理工具，负责下载安装依赖                      | 采用 pip + requirements.txt  |
| ORM                  | Object-Relational Mapping             | 把数据库的「表」映射成代码里的「类」，用写代码代替手写 SQL | SQLAlchemy 就是 ORM          |
| SQLAlchemy           | —                                     | Python 最主流的 ORM 库                                     | 定义数据模型、执行查询       |
| Pydantic             | —                                     | 数据校验库：自动检查前端传来的数据格式对不对               | 校验请求/响应数据            |
| 数据校验             | Validation                            | 检查输入是否合法（如金额不能为负）                         | 后端用 Pydantic 校验所有入参 |
| 序列化 / 反序列化    | Serialization                         | 把程序里的对象转成可传输文本（如 JSON），反之亦然          | Pydantic 负责请求/响应转换   |
| 路由                 | Router                                | 定义「访问哪个网址 → 执行哪段代码」的映射                  | `api/v1/` 下的各模块         |
| 依赖注入             | Dependency Injection                  | 框架自动把需要的对象「递」给你的函数，不用自己手动创建     | FastAPI 用它实现 JWT 鉴权    |
| 定时任务             | Scheduled Task                        | 按固定时间自动执行的任务（如每天凌晨跑一次）               | 逾期检测、还款提醒           |
| APScheduler          | —                                     | Python 的定时任务库                                        | 实现逾期检测                 |
| 数据库迁移           | Migration                             | 用脚本管理表结构变化，可版本化、可回滚                     | Alembic（可选）              |
| 统一返回结构         | —                                     | 所有接口都返回同一种格式，方便前端统一处理                 | `{code, message, data}`      |
| Swagger UI / OpenAPI | —                                     | 自动生成的网页版接口文档，可直接在网页上试调接口           | FastAPI 的 `/docs` 页面      |
| seed 数据            | —                                     | 初始化时预先塞进数据库的测试数据                           | 管理员账号、测试产品         |

---

## 4. 前端技术

| 术语         | 英文/全称   | 通俗解释                                   | 在本项目中                 |
| ------------ | ----------- | ------------------------------------------ | -------------------------- |
| Vue 3        | —           | 主流前端框架，用模板语法描述页面           | 本项目前端框架             |
| Vite         | —           | 前端构建 / 开发服务器工具，启动速度快      | 前端工程构建               |
| Element Plus | —           | 现成的 UI 组件库（表格、表单、弹窗等）     | 快速搭出用户端与管理端界面 |
| Pinia        | —           | Vue 的状态管理库，集中存放「全局数据」     | 保存当前用户信息、权限     |
| Vue Router   | —           | 前端路由：决定不同网址显示哪个页面         | 页面跳转                   |
| 路由守卫     | Route Guard | 进入页面前先做检查（如未登录就踢回登录页） | 前端权限控制               |
| Axios        | —           | 前端发 HTTP 请求的库                       | 调用后端接口               |
| 拦截器       | Interceptor | 在每次请求/响应前后自动加一段处理逻辑      | 自动携带 JWT、统一报错     |
| 组件         | Component   | 可复用的界面积木（如一个表格、一个弹窗）   | 页面由组件拼装而成         |
| Node.js      | —           | 让 JavaScript 能在浏览器之外运行的环境     | 前端工具链的运行基础       |
| npm / pnpm   | —           | JavaScript 的包管理器                      | 安装前端依赖               |
| Mock 数据    | —           | 后端接口还没好时，前端先用假数据开发       | 前端并行开发的手段         |

---

## 5. 数据库与数据

| 术语             | 英文/全称                   | 通俗解释                                           | 在本项目中                              |
| ---------------- | --------------------------- | -------------------------------------------------- | --------------------------------------- |
| 关系型数据库     | RDBMS                       | 用「表格」存数据的数据库，表与表之间可建立关联     | MySQL                                   |
| MySQL            | —                           | 最流行的开源关系型数据库                           | 存放核心业务数据                        |
| Redis            | —                           | 内存数据库，读写极快，常用来做缓存                 | 存验证码、令牌黑名单                    |
| 非关系型数据库   | NoSQL                       | 不用表格结构的数据库（如 MongoDB）                 | ⚠️ 不采用：借贷业务强依赖事务与金额精度 |
| 表 / 字段 / 记录 | Table / Column / Row        | 表 = 一张二维表；字段 = 一列；记录 = 一行          | 见《03-数据库设计》                     |
| 主键             | Primary Key, PK             | 能唯一标识一行数据的字段                           | 每张表的 `id`                           |
| 外键             | Foreign Key, FK             | 指向另一张表主键的字段，用来建立关联               | 如 `user_id`                            |
| 索引             | Index                       | 给字段建「目录」，让查询更快                       | 见《03》§5                              |
| ER 图            | Entity-Relationship Diagram | 用图表示「有哪些实体、彼此是什么关系」             | 见《03》§2                              |
| 事务             | Transaction                 | 一组操作「要么全成功，要么全失败」，保证数据不出错 | 放款时扣款 + 生成计划必须一起成功       |
| 回滚             | Rollback                    | 事务失败时，把已做的改动全部撤销                   | 出错时恢复原状                          |
| 悲观锁 / 行级锁  | Pessimistic Lock            | 先锁住那行数据再改，别人只能等着                   | `SELECT ... FOR UPDATE`                 |
| 乐观锁           | Optimistic Lock             | 改之前先比对版本号，不一致说明被别人改过，就重试   | 防止并发超扣，见《03》§4.3              |
| 并发             | Concurrency                 | 多个人同时操作同一份数据                           | 需要防止超额扣款                        |
| 精度 / DECIMAL   | Precision                   | 金额计算不能有误差，必须用定点数而不是浮点数       | 金额用 `DECIMAL(18,2)` 与 `Decimal`     |
| 浮点数           | float                       | 计算机表示小数的方式，存在微小误差                 | ⚠️ 严禁用于金额运算                     |
| 分页             | Pagination                  | 数据太多时一页只显示一部分                         | `limit` / `offset`                      |
| 缓存             | Cache                       | 把常用数据放内存里，减少查询数据库                 | Redis                                   |
| 状态机           | State Machine               | 用「状态 + 允许的转移」来描述业务流程              | 借款申请状态，见《02》§5.2              |
| 逻辑删除         | Soft Delete                 | 不真删数据，只标记 `deleted=1`                     | 表设计可选项                            |

---

## 6. 安全与鉴权

| 术语       | 英文/全称               | 通俗解释                                             | 在本项目中                         |
| ---------- | ----------------------- | ---------------------------------------------------- | ---------------------------------- |
| 认证       | Authentication          | 确认「你是谁」（登录）                               | 用户登录                           |
| 授权       | Authorization           | 确认「你能干什么」（权限）                           | 普通用户 vs 管理员                 |
| JWT        | JSON Web Token          | 一种登录凭证（令牌），服务器签发后前端每次请求带上它 | 本项目的登录鉴权方式               |
| 令牌       | Token                   | 证明「我已登录」的一串字符串                         | 放在请求头 `Authorization` 中      |
| 令牌黑名单 | Token Blacklist         | 把已注销的令牌记下来，让它立即失效                   | 存 Redis                           |
| 哈希       | Hash                    | 把密码变成不可逆的乱码，数据库只存乱码               | 密码存储                           |
| BCrypt     | —                       | 一种专为密码设计的哈希算法（自带「加盐」）           | 配合 passlib 使用                  |
| 加盐       | Salt                    | 给密码掺入随机字符再哈希，防止被对照表破解           | BCrypt 自动处理                    |
| 验证码     | CAPTCHA                 | 证明操作的是真人，防止机器人                         | 注册时发送，存 Redis 且 5 分钟过期 |
| 越权校验   | —                       | 防止用户访问本不属于他的数据                         | 后端每个接口都校验                 |
| 脱敏       | Masking                 | 敏感信息显示时打码，如 `138****8888`                 | 身份证、手机号、银行卡号           |
| 实名认证   | KYC, Know Your Customer | 核验用户真实身份                                     | 认证通过后才能借款                 |
| 环境变量   | Environment Variable    | 存放配置（如数据库密码）而不写死在代码里             | 数据库连接、密钥配置               |

---

## 7. 工程协作与工具链

| 术语                    | 英文/全称                          | 通俗解释                                           | 在本项目中                       |
| ----------------------- | ---------------------------------- | -------------------------------------------------- | -------------------------------- |
| Git                     | —                                  | 代码版本管理工具，记录每次改动，支持多人协作       | 全程使用                         |
| 仓库                    | Repository                         | 用 Git 管理的项目目录                              | 指本项目                         |
| 分支                    | Branch                             | 从主线分出来的独立开发线                           | `main` / `dev` / `feature`       |
| 主分支 / 集成分支       | —                                  | `main` = 稳定版；`dev` = 日常集成分支              | 见《04》§4                       |
| 提交                    | Commit                             | 把一次改动「存档」，并附上说明                     | `git commit`                     |
| 推送 / 拉取             | Push / Pull                        | 把本地提交传到远端 / 从远端取回                    | `git push` / `git pull`          |
| 合并 / 冲突             | Merge / Conflict                   | 把分支合到一起；两人改了同一处就会冲突，需手动解决 | 每天把 feature 合并回 dev        |
| .gitignore              | —                                  | 告诉 Git 哪些文件不用管（如临时文件、密钥）        | 已添加                           |
| .gitattributes          | —                                  | 告诉 Git 各类文件的处理规则                        | 已统一换行符为 LF                |
| 换行符                  | LF / CRLF                          | 文本换行的记号差异：LF 是 `\n`，CRLF 是 `\r\n`     | 全仓库统一为 LF，避免协作报错    |
| IDE                     | Integrated Development Environment | 写代码用的编辑器 / 集成开发环境                    | PyCharm（后端）、VS Code（前端） |
| Postman / Apifox        | —                                  | 用来单独测试接口的工具                             | 调试接口                         |
| Docker / Docker Compose | —                                  | 把程序和依赖打包成「容器」，一键复现运行环境       | 进阶可选的部署方式               |
| 联调                    | Integration Debugging              | 前后端各自写完后接在一起跑通                       | M2 起每 2 天一次                 |
| 里程碑                  | Milestone                          | 项目里的关键节点                                   | M1 ~ M4                          |
| MVP                     | Minimum Viable Product             | 最小可行产品：只做最重要、必须有的功能             | P0 功能清单                      |

---

## 8. 借贷业务术语

| 术语            | 英文/全称            | 通俗解释                               | 在本项目中          |
| --------------- | -------------------- | -------------------------------------- | ------------------- |
| 本金            | Principal            | 借出去的钱本身                         | 借款金额            |
| 利息            | Interest             | 用钱所付的「租金」                     | 按利率计算          |
| 利率 / 年化利率 | Interest Rate / APR  | 利息的比率，通常按年表示               | 产品的年利率区间    |
| 月利率          | Monthly Rate         | 年利率 ÷ 12                            | 用于计算每期利息    |
| 等额本息        | Equal Installment    | 每期还款总额相同，前期利息多、本金少   | 三种还款方式之一    |
| 等额本金        | Equal Principal      | 每期还的本金相同，利息逐期减少         | 三种还款方式之一    |
| 先息后本        | Interest-first       | 平时只还利息，最后一期一次性还本金     | 三种还款方式之一    |
| 还款计划        | Repayment Schedule   | 放款时生成的清单：每期该还多少、何时还 | 见《03》§3.5        |
| 逾期            | Overdue              | 到了还款日仍未还款                     | 触发罚息            |
| 罚息            | Penalty Interest     | 逾期产生的额外罚金                     | 按日计算            |
| 提前还款        | Early Repayment      | 未到期就把剩余本金还清                 | 可能收取违约金      |
| 违约金          | Prepayment Penalty   | 提前还款时按约定收取的费用             | 产品中可配置        |
| 授信额度        | Credit Line          | 平台最多允许你借多少                   | 本期使用固定默认值  |
| 信用评分 / 等级 | Credit Score / Level | 根据收入、还款记录等算出的信用好坏     | 本期预留，综设2实现 |
| 风控            | Risk Control         | 控制放贷风险的一整套规则               | 黑名单拦截本期实现  |
| 审批            | Approval             | 管理员决定这笔借款批不批               | 状态机核心环节      |
| 放款            | Disbursement         | 审批通过后把钱打给借款人               | 同时生成还款计划    |
| 结清            | Settlement           | 本金利息全部还完，借款结束             | 业务终态            |
| 催收            | Collection           | 对逾期用户的提醒与追讨                 | 逾期管理模块        |
| 黑名单          | Blacklist            | 被拉黑的用户，申请会被自动拒绝         | 见《03》§3.11       |
| 账户流水        | Account Flow         | 账户每一笔金额变动的记录               | 见《03》§3.8        |
| 冻结金额        | Frozen Amount        | 暂时不可用、但仍属于你的钱             | 账户表字段          |

---

## 9. 测试与交付

| 术语                | 英文/全称                 | 通俗解释                             | 在本项目中               |
| ------------------- | ------------------------- | ------------------------------------ | ------------------------ |
| 单元测试            | Unit Test                 | 针对一个函数或一小段逻辑写的自动检查 | 建议覆盖利息计算         |
| 集成测试            | Integration Test          | 测试多个模块接在一起是否正常         | 放款流程                 |
| 验收测试            | UAT, User Acceptance Test | 按需求逐条验证功能是否达标           | M1 ~ M4 检查点           |
| 测试用例            | Test Case                 | 一条具体的「输入 → 期望输出」        | 覆盖典型金额场景         |
| 部署                | Deployment                | 把程序放到能长期运行的环境上         | 本地 `uvicorn` 或 Docker |
| 生产环境 / 开发环境 | Production / Development  | 正式对外服务的环境 / 开发调试的环境  | 本项目以本地演示为主     |
